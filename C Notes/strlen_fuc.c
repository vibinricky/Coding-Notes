#include <stdio.h>
#include <string.h>

void main()
{
    char str[20] = "Vit Vellore";
    puts(str);

    // strlen();
    int length = strlen(str);
    printf("The of the given string is:%d \n",length);

}