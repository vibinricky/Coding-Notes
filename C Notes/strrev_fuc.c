#include <stdio.h>
#include <string.h>

void main()
{
    char str[20] = "Vit Vellore";
    puts(str);

    // strrev();
    printf("The reverse of the string is: %s \n",strrev(str));
    puts(str);


}